---
import Layout from '../layouts/Layout.astro';
import Card from '../components/Card.astro';
import { getCollection } from 'astro:content';

const projects = await getCollection('projects');
---

<Layout title="Mis Proyectos | Josue Nolasco">
  <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-10">
    <!-- Header Section -->
    <div class="mb-16 text-center reveal-content">
      <h2 class="text-rose-500 font-semibold tracking-wide uppercase text-sm mb-2">Portfolio</h2>
      <h1 class="text-4xl md:text-6xl font-extrabold mb-4 bg-gradient-to-r from-rose-500 via-purple-500 to-indigo-500 bg-clip-text text-transparent">
        Mis Proyectos
      </h1>
      <p class="text-gray-400 text-lg md:text-xl max-w-2xl mx-auto">
        Una colección de trabajos en los que he aplicado mis habilidades de desarrollo, desde aplicaciones web hasta sistemas complejos.
      </p>
    </div>

    <!-- Grid Layout -->
    <div 
      class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 justify-items-center opacity-0 translate-y-10 transition-all duration-1000 ease-out" 
      id="animated-section"
    >
      {projects.map((project) => (
        <Card 
          title={project.data.title}
          description={project.data.description}
          icon={project.data.icon}
          href={`/projects/${project.slug}`}
        />
      ))}
    </div>
  </main>
</Layout>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    const section = document.getElementById('animated-section');
    if (section) {
      setTimeout(() => {
        section.classList.remove('opacity-0', 'translate-y-10');
      }, 100);
    }
  });
</script>

<style>
  .reveal-content {
    animation: reveal-anim 0.8s ease-out forwards;
  }

  @keyframes reveal-anim {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>